package <tmpl_var name = base_name>;

# File name: <tmpl_var name = module_name>.
# Installation path: <tmpl_var name = module_path>.
# Code generated by Class::DBI::Schema2Code.

use base 'Class::DBI';

use strict;
use warnings;

use DBIx::Admin::TableInfo;

# -----------------------------------------------
# Convention:
# Table name: x
# Primary key name: x_id
# Move this column to the front of the list,
# so sub-classes can call columns() safely.

sub column_names
{
	my($self, $table_name) = @_;
	my($dbh)	= __PACKAGE__ -> db_handles();
	my($column)	= grep{! /^${table_name}_id$/} DBIx::Admin::TableInfo -> new(dbh => $dbh) -> columns($table_name);

	unshift @$column, "${table_name}_id";

	$column;

}	# End of column_names.

# -----------------------------------------------
# Convention:
# If the driver is Pg, sub-classes call sequence().

sub get_driver
{
	my($self) = @_;

	'mysql';

}	# End of get_driver.

# -----------------------------------------------

__PACKAGE__ -> set_db('Main', ('dbi:' . __PACKAGE__ -> get_driver() . ':mids'), '<tmpl_var name = username>', '<tmpl_var name = password>');

1;
